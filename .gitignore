result
result-*
__pycache__/
*.pyc

# docker deployment dirs (created at deploy time; see README "Usage with Docker")
/models/
/configs/
/webui/
# local deploy override (host-specific mounts); ship the .example instead
/docker-compose.override.yml
.venv/
.direnv/
