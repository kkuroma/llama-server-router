[pytest]
# core holds the fixtures, which ../conftest.py registers, so it is not collected
norecursedirs = core
markers =
    gpu: needs a real GPU and the weights named in configs.toml
    slow: loads and swaps real models, so it runs for minutes
addopts = -ra -m "not gpu"
